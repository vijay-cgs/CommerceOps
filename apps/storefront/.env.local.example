# The zone delegates authenticated BFF/API requests to the shell.
WEB_SHELL_URL=http://localhost:3000

# Must match the shell so cookies forwarded through the shell can be verified.
AUTH_JWT_SECRET=replace-with-a-random-48-character-string-value
