AUTH_JWT_SECRET=replace-with-a-random-48-character-string-value

# Optional Multi-Zone development composition. Leave unset to use the current
# monolithic web app while routes are migrated into the independent zones.
STOREFRONT_ZONE_URL=http://localhost:3001
ADMIN_ZONE_URL=http://localhost:3003
